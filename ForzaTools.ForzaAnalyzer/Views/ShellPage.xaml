<Page
    x:Class="ForzaTools.ForzaAnalyzer.Views.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:ForzaTools.ForzaAnalyzer.ViewModels"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="FileItemTemplate" x:DataType="vm:FileViewModel">
            <NavigationViewItem Content="{x:Bind FileName}" Icon="Document" Tag="{x:Bind}"/>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <NavigationView x:Name="NavView" 
                PaneDisplayMode="Left"
                IsSettingsVisible="False"
                SelectionChanged="NavView_SelectionChanged"
                MenuItemTemplate="{StaticResource FileItemTemplate}"
                MenuItemsSource="{x:Bind ViewModel.Files, Mode=OneWay}">
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home" Content="Home" Tag="Home"/>
                <NavigationViewItemSeparator/>
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame"/>
        </NavigationView>

        <Grid Background="#80000000" 
              Visibility="{x:Bind BooleanToVisibility(ViewModel.IsBusy), Mode=OneWay}" 
              HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Canvas.ZIndex="100">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
                <ProgressRing IsActive="True"/>
                <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" Foreground="White"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>