<Page
    x:Class="ForzaTools.ForzaAnalyzer.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d">

    <Grid AllowDrop="True" Drop="Grid_Drop" DragOver="Grid_DragOver">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
            <FontIcon Glyph="&#xE8B7;" FontSize="64" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
            <TextBlock Text="ForzaTools Analyzer" Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center"/>
            <TextBlock Text="Drag and drop .modelbin, .carbin, or .zip files here" Style="{StaticResource BodyTextBlockStyle}" HorizontalAlignment="Center" Opacity="0.7"/>

            <Button Content="Open Files" Command="{x:Bind ViewModel.OpenFilesCommand}" 
                    Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Center" Width="200" Height="40"/>

            <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center">
                <Button Content="Create ModelBin" Command="{x:Bind ViewModel.CreateModelBinCommand}" 
                        Width="140" Height="35"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>